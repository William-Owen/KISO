// {{name}}

import React from "react"
import "@testing-library/jest-dom/extend-expect"
{{#if useRedux}}import renderWithRedux from "../../../utilities/rederWithRedux"{{else}}import { render } from "@testing-library/react"{{/if}}
import {{name}} from "./{{name}}"

// eslint-disable-next-line no-undef
it("renders without crashing", () => {

	{{#if useRedux}}renderWithRedux(<{{name}} />){{else}}render(<{{name}} />){{/if}}

})
